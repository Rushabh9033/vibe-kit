# Session: 2026-08-21 — main

## Goal
Land the rest of the v0.2 hard-gate surface: `/vibe-claim-check` as the real
ship-time gate, mutation testing as Rank 1, and AI-assisted intake. All five
items on the priority queue.

## Branch / state
`main` — clean, pushed to origin (`63ccb40`)

## Done
- **#3 ship-gate swap** (commits `13f0fa5`, `9b0c2e4`): `vibe-claim-check`
  is now the ship-time gate. Pre-push hook rewired from verify→claim-check.
  Exit-code mapping documented; 18-assertion test added.
- **#4 mutation testing as Rank 1** (commit `63ccb40`): new `kit/bin/vibe-mutate`
  with language auto-detection (mutmut/Stryker/PIT), threshold-gated (default
  70), wired into `vibe-claim-check` as a post-test step. Mutation FAIL→PARTIAL.
- **#5 AI-assisted intake** (commit `63ccb40`): `vibe-spec-intake --ai` flag
  invokes `claude --print` with a kit-owned prompt template. Falls back to
  template on any failure.
- Specs written: `docs/requirements/{vibe-mutate,vibe-spec-intake-ai}/spec.md`.
- Two manual handoffs:
  `docs/handoffs/2026-08-21-ship-gate-swap.md` and
  `docs/handoffs/2026-08-21-vibe-mutate-and-ai-intake.md`.

## In progress
(none)

## Blocked
(none)

## Files touched
- kit/bin/vibe-mutate (new), kit/bin/vibe-claim-check (wired), kit/bin/vibe-spec-intake (--ai)
- kit/prompts/spec-intake.md (new prompt template)
- kit/CLAUDE.md, kit/commands/vibe-spec.md, README.md, CHANGELOG.md
- docs/requirements/{vibe-mutate,vibe-spec-intake-ai}/spec.md (new)
- docs/handoffs/2026-08-21-ship-gate-swap.md, 2026-08-21-vibe-mutate-and-ai-intake.md
- tests/weapons/test-vibe-mutate.sh, test-spec-intake-ai.sh (new), test-pre-push.sh

## Decisions made
- Verify is mid-dev only; claim-check is ship-only. Two commands, two jobs,
  two speeds. (See ship-gate-swap handoff.)
- Mutation testing is ship-time only — too slow for mid-dev verify.
- AI-assisted intake is opt-in (`--ai`); default template mode unchanged.
- `VIBE_SHIP_OVERRIDE=1` lifts PARTIAL but never FAIL — same semantic as before.

## Decisions needed
(none)

## Verification
- 173 / 173 assertions across 6 test suites, 0 failures.
- `bash -n` clean on all modified shell scripts.
- Mutation stub parsers verified against fake mutmut/Stryker/mvn on PATH.

## Next steps for next session
1. The hard-gate surface for v0.2 is complete. Natural next moves:
   - One-command fresh-project bootstrap (intake --ai + arm)
   - Differential mutation (mutate only changed lines)
   - Multi-CLI AI support (codex, gemini)
2. Check for new `vibe-kit` issues / priorities from the project owner.
3. Re-run `./kit/bin/vibe-verify` after the next change to confirm AC↔code↔tests
   still hold.
